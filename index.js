class Formatter {
  //add static methods here
  static capitalize(string) {
    return string.charAt(0).toUpperCase() + string.slice(1);
}
  static sanitize(string) {
    // string to array. remove caracter if not alphanumeric or dash single quote and space. join array to string.
  }
  static titleize() {

  }

}
